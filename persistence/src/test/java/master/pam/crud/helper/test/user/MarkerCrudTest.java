package master.pam.crud.helper.test.user;

public class MarkerCrudTest {
    public static void main(String[] args) {

        // CrudHelper.init();
        // UserEntity tibi = CrudHelper.getUser( "tibi" );
        // UserEntity ale = CrudHelper.getUser( "ale" );
        //
        // List< MarkerEntity > tibiMarkers = CrudHelper.getMarkers( tibi.getName()
        // );
        // List< MarkerEntity > aleMarkers = CrudHelper.getMarkers( ale.getName() );

        // for ( MarkerEntity m : aleMarkers ) {
        // MarkerEntity marker = new MarkerEntity();
        // marker.setUserId( tibi );
        // marker.setDate( m.getDate() );
        // marker.setNote( m.getNote() );
        // marker.setLatitude( m.getLatitude() );
        // marker.setLongitude( m.getLongitude() );
        // marker.setName( m.getName() );
        // CrudHelper.save( marker );
        // }

        // MarkerEntity marker = new MarkerEntity();
        // marker.setUserId( tibi );
        // marker.setDate( new Date() );
        // marker.setNote( "Test from " + marker.getDate() );
        //
        // CrudHelper.save( marker );
        //
        // List< MarkerEntity > markers = CrudHelper.getMarkers( user.getEmail() );
        //
        // for ( MarkerEntity m : markers ) {
        // System.out.println( m.toString() );
        // }

        // CrudHelper.end();
    }
}
